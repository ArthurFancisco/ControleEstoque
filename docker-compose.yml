services:
  postgres:
    image: postgres:16-alpine
    container_name: flowstock-postgres
    environment:
      POSTGRES_DB: flowstock
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postdba
    ports:
      - "5432:5432"
    volumes:
      - flowstock-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d flowstock"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
    container_name: flowstock-backend
    profiles: ["app"]
    environment:
      DB_URL: jdbc:postgresql://postgres:5432/flowstock
      DB_USERNAME: postgres
      DB_PASSWORD: postdba
      JWT_SECRET: flowstock-docker-development-secret-change-me-please-32
      CORS_ALLOWED_ORIGINS: http://localhost:5173,http://localhost:3000
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
    container_name: flowstock-frontend
    profiles: ["app"]
    environment:
      VITE_API_URL: http://localhost:8080
    ports:
      - "5173:80"
    depends_on:
      - backend

volumes:
  flowstock-postgres-data:
